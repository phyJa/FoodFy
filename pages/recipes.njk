<!DOCTYPE html>
<html lang="pt_BR">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Receitas</title>

        <!-- Favicon -->
        <link rel="shortcut icon" href="/global/assets/favicon.ico" type="image/x-icon" />
        <link rel="icon" href="/global/assets/favicon.ico" type="image/x-icon" />

        <!-- Style -->
        <link rel="stylesheet" href="/global/global.css" />
        <link rel="stylesheet" href="/global/globalResponsivity.css" />
    </head>
    <body>
        <header>
            <div class="logoContainer">
                <a href="/">
                    <img 
                        src="../../assets/logo.png"  
                        alt="FoodFy logo" 
                        title="Home" 
                    />
                </a>  
            </div>

            <div class="linksContainer">
                <a href="/about">Sobre</a>

                <a href="" id="active">Receitas</a>
            </div>
        </header>

        <section>
            <h3>Receitas</h3>

            <div class="displayRecipes">
                
                {% for recipe in recipes %}

                    <div class="recipe">

                        <div class="imgContainer">

                            <img src="{{recipe.imgSrc}}" alt="Image of {{recipe.recipeTitle}}" />

                        </div>

                        <div class="recipeDescription">

                            <h4 class="recipeTitle"> {{ recipe.recipeTitle }} </h4>

                            <p class="recipeOwner"> por {{ recipe.recipeOwner }} </p>

                        </div>

                    </div>

                {% endfor %}

                {% for recipe in recipes %}

                    <div class="recipe">

                        <div class="imgContainer">

                            <img src="{{recipe.imgSrc}}" alt="Image of {{recipe.recipeTitle}}" />

                        </div>

                        <div class="recipeDescription">

                            <h4 class="recipeTitle"> {{ recipe.recipeTitle }} </h4>

                            <p class="recipeOwner"> por {{ recipe.recipeOwner }} </p>

                        </div>

                    </div>

                {% endfor %}

            </div>
            
        </section>

        <footer>
            Todos os direitos reservados, receitas FoodFy.
        </footer>

        <div class="modalOverlay notDisplay">
            <div class="modal">
                <div class="modalContent">
                    <div class="imgContainer">
                        <img src="" alt="" />
                    </div>

                    <div class="textContainer">
                        <h4 class="recipeTitle"></h4>

                        <p class="recipeOwner"></p>
                    </div>
                </div>

                <div class="closeModal">
                    <button> Fechar </button>
                </div>
            </div>
        </div>

        <script src="/global/global.js"></script>
    </body>
</html>